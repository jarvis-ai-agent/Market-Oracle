{
  "name": "market-oracle",
  "private": true,
  "workspaces": ["frontend"],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "dev:backend": "cd backend && uvicorn main:app --reload --port 8000",
    "install:all": "npm install && cd backend && pip3 install -r requirements.txt",
    "build": "npm run build --workspace=frontend"
  }
}
